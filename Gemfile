# This file is used for declaring development-time dependencies only.
# For runtime dependencies, they'll also need to be added to the Thorfile
# so they are included in the generated Gemspec

# Prevent system gems from being loadable during the spec run
disable_system_gems

# Add gems required for the specs to run here:
gem "rspec"
